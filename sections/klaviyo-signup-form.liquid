<div class="klaviyo-form-Whvmr4"></div>

<script>
  // Listen for Klaviyo form events
  document.addEventListener('klaviyoForms', function (event) {
    // Only process for your specific form
    if (event.detail.formId == 'Whvmr4') {
      // When the form is about to be submitted
      if (event.detail.type == 'submit') {
        // Add or update the custom property
        event.detail.klaviyoData.$hvco_type = '{{ page.title }}';
        console.log('Added custom property to submission: {{ page.title }}');
      }
    }
  });
</script>
